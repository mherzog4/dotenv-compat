[package]
name = "dotenv-compat"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Bug-for-bug compatible Rust port of the JavaScript dotenv parser"
license = "MIT"
readme = "readme.md"
keywords = ["dotenv", "env", "environment", "config", "dotfiles"]
categories = ["config"]
exclude = ["scripts/node_modules", "scripts/fuzz-failures.json", "port.md"]

[lib]
name = "dotenv_compat"
path = "src/lib.rs"

[dependencies]
